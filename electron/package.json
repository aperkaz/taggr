{
  "name": "taggr",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": ">=14.15.4"
  },
  "scripts": {
    "postinstall": "electron-builder install-app-deps",
    "watch": "electron .",
    "watch:backend": "yarn build:backend -w --preserveWatchOutput",
    "build:backend": "rimraf build-backend; mkdir build-backend; cp ./src/backend/server-dev.html ./build-backend/server-dev.html; tsc",
    "build:frontend": "rimraf build-frontend; mkdir build-frontend; (cd ../frontend; yarn && yarn build); cp -R ../frontend/build/. ./build-frontend",
    "build": "yarn build:frontend; yarn build:backend; yarn dist",
    "pack": "electron-builder --dir",
    "dist": "rimraf ./dist && electron-builder"
  },
  "build": {
    "appId": "Taggr",
    "mac": {
      "category": "your.app.category.type"
    }
  },
  "dependencies": {
    "electron-is-dev": "^1.1.0",
    "node-ipc": "^9.1.1",
    "sharp": "^0.28.3",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^15.6.1",
    "electron": "^5.0.4",
    "electron-builder": "^22.10.5",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.2"
  }
}
