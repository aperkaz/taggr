{
  "name": "shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index",
  "types": "src/index.ts",
  "scripts": {
    "prebuild": "rimraf dist/",
    "build": "yarn compile",
    "compile": "tsc -p tsconfig.build.json",
    "watch": "yarn compile -w --preserveWatchOutput",
    "test": "jest --verbose --runInBand",
    "test:ci": "jest --forceExit --ci --runInBand"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^27.0.6",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.5.6",
    "typescript": "^4.2.4"
  }
}
